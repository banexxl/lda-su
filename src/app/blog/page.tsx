import { BlogView } from 'src/sections/view/blog-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'LDA Subotica: Blog Post',
};

export default function BlogPage() {
  return <BlogView />;
}
